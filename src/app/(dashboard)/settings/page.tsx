import EmptyState from '@/components/common/EmptyState'

export default function SettingsPage() {
  return <EmptyState icon="⚙️" title="Settings" description="Auth, preferences, and workspace settings can be added here next." />
}
